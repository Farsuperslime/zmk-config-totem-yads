# Deep sleep kept ON for the halves to preserve battery life.
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000

# Match the dongle's BLE connection settings (central + peripherals should agree).
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
